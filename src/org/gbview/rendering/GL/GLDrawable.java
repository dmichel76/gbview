package org.gbview.rendering.GL;

import javax.media.opengl.GL;

/**
 * Interface for a drawable object used in @link{abstractGLObject}.
 */
public interface GLDrawable
{
    public void draw(GL gl);
}
