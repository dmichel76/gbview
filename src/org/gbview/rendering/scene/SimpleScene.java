package org.gbview.rendering.scene;

import org.gbview.model.Model;

import javax.media.opengl.GL;

/**
 *
 */
public class SimpleScene extends AbstractScene
{
    public SimpleScene(Model model) {
        super(model);
    }
    // empty as there is nothing to add that does not already exists in the abstract class
}
